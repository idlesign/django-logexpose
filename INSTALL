django-logexpose installation
=============================


Python ``setuptools`` package is required to install django-logexpose.


Use the following command line to install django-logexpose from sources directory (containing setup.py):

    easy_install .

or

    python setup.py install


Alternatively you can install an up-to-date version of django-logexpose from PyPI:

    easy_install django-logexpose


Note: `easy_install` is bundled with ``setuptools``.
